var db = require("../models");

module.exports = function(app) {
	// get story
	// find story by ID
	// find first snippet by checking all level 1 snippets for the one with the parentID that matches the story ID

	// get branches
	// find all non level 1 snippets who's parent id match the current snippet's id
};